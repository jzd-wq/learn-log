# date-list

CS61B Lists 和 References 学习代码。

当前代码实践包括：

- 使用节点和 `next` 引用构造链表；
- 使用 `size()` 计算链表长度；
- 使用 `other_size()` 通过循环计算链表长度；
- 使用 `get(index)` 按索引访问节点中的数据。

## 运行

```bash
javac src/Main.java
java -cp src Main
```
